type Query {
  bookA(name: String): [Book]
  RunPythonCode(code: String): String
  Signup(user_name:String, user_password:String): Int
  Signin(user_name:String, user_password:String): String
}

type Book {
  id: ID
  name: String
  page: Int
  author: String
}
